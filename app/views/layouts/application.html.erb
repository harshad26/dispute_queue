<!DOCTYPE html>
<html>
  <head>
    <title>Dispute Queue</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <style>
      body { font-family: system-ui, -apple-system, sans-serif; background: #f9fafe; margin: 0; padding: 0; color: #333; }
      nav { background: #fff; padding: 15px 30px; border-bottom: 1px solid #eaeaea; display: flex; justify-content: space-between; align-items: center; }
      nav a { text-decoration: none; color: #555; font-weight: 500; margin-left: 20px; }
      nav .brand { font-weight: bold; color: #000; font-size: 1.1em; margin-left: 0; }
      .container { max-width: 1000px; margin: 40px auto; padding: 0 20px; }
      .btn { padding: 8px 16px; border-radius: 6px; text-decoration: none; font-size: 14px; border: none; cursor: pointer; }
      .btn-primary { background: #000; color: #fff; }
      .card { background: #fff; border: 1px solid #eaeaea; border-radius: 8px; padding: 20px; margin-bottom: 20px; }
      table { width: 100%; border-collapse: collapse; }
      th { text-align: left; color: #888; font-weight: 500; padding: 10px 0; border-bottom: 1px solid #eee; }
      td { padding: 15px 0; border-bottom: 1px solid #f5f5f5; }
      .status-badge { padding: 4px 8px; border-radius: 12px; font-size: 12px; font-weight: 600; }
      .status-open { background: #fff4e5; color: #b76e00; }
      .status-evidence_submitted { background: #e3f2fd; color: #0277bd; }
      .status-closed_won { background: #e8f5e9; color: #2e7d32; }
      .status-closed_lost { background: #ffebee; color: #c62828; }
    </style>
  </head>

  <body>
    <nav>
      <a href="/disputes" class="brand">DisputeQueue</a>
      <div>
        <% if authenticated? %>
          <span style="color: #888; margin-right: 15px; font-size: 14px;"><%= current_user.email_address %></span>
          <a href="/disputes">Queue</a>
          <a href="/reports/money_math">Money</a>
          <a href="/reports/time_zone">Time Zones</a>
          <a href="/reports/daily_volume">Daily Volume</a>
          <a href="/reports/time_to_decision">Time To Decision</a>
          <a href="/webhooks/simulated/new">Simulator</a>
          <%= button_to "Sign out", session_path, method: :delete, style: "background:none; border:none; color:#555; font-weight:500; font-size:16px; font-family:inherit; cursor:pointer;" %>
        <% else %>
          <a href="/webhooks/simulated/new">Simulator</a>
          <%= link_to "Sign in", new_session_path %>
        <% end %>
      </div>
    </nav>
    
    <main class="container">
      <% if flash[:notice] %>
        <div style="background: #e8f5e9; color: #2e7d32; padding: 12px; border-radius: 6px; margin-bottom: 20px;">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div style="background: #ffebee; color: #c62828; padding: 12px; border-radius: 6px; margin-bottom: 20px;">
          <%= flash[:alert] %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
